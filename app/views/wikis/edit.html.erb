<h2>edit your wiki</h2>

<%= form_for :wiki, url: wiki_path(@wiki), method: :patch do |f| %>

  <% if @wiki.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@wiki.errors.count, "error") %> prohibited
        this wiki from being saved:
      </h2>
      <ul>
        <% @wiki.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "input-field" %>
  </p>
  <p>
    <%= f.submit %>

  </p>

<% end %>

<%= link_to 'Back', wikis_path %>
